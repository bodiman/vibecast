{
  "mcpServers": {
    "vibecast-frameworks": {
      "command": "node",
      "args": [
        "--import",
        "tsx/esm",
        "/ABSOLUTE/PATH/TO/vibecast/src/mcp/creao-server.ts"
      ],
      "env": {
        "DATABASE_URL": "postgresql://user:password@host:port/database"
      },
      "description": "VibeCast Framework System - Create, query, and manage graph frameworks"
    }
  }
}
